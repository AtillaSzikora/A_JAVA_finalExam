package web;

import entity.Person;
import java.util.Comparator;

public class RateComparator implements Comparator<Person> {

    @Override
    public int compare(Person person1, Person person2) {return 0;}
}
